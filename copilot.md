# GitHub Copilot Instructions - GuitarTools

> **Operational Mandates:** All AI agents MUST follow the rules in [AGENTS.md](AGENTS.md).

This file provides specific context for GitHub Copilot.

## Context

- **Architecture:** Vanilla JS, Web Components, SVG.
- **Conventions:** Pure logic in `*Logic.js`, UI in `js/components/` or controllers in `js/games/`.
- **Testing:** Vitest for logic tests in `tests/unit/`.
